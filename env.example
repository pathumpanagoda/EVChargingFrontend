# Development environment variables
VITE_API_BASE_URL=https://localhost:7090

# Note: In development, Vite proxy will handle /api requests
# The proxy is configured in vite.config.js to forward /api/* to https://localhost:7090
# Copy this file to .env to use these settings
